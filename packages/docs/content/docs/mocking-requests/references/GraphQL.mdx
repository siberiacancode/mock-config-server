---
title: GraphQL
---

import { TypeTable } from 'fumadocs-ui/components/type-table';
 
## GraphqlConfig

<TypeTable
  type={{
    baseUrl: {
      description: 'The base URL',
      type: '`/${string}`'
    },
    configs: {
      description: 'Array of GraphQL request configurations',
      type: 'GraphQLRequestConfig[]',
      typeDescriptionLink: '#GraphQLRequestConfig',
      required: true
    },
    interceptors: {
      description: 'Optional interceptors applied to GraphQL handlers',
      type: "Interceptors<'graphql'>",
      typeDescriptionLink: './Interceptors'
    }
  }}
/>

<a id='GraphQLRequestConfig'></a>
## GraphQLRequestConfig

<TypeTable
  type={{
    operationType: {
      description: 'Operation type (query or mutation)',
      type: 'GraphQLOperationType',
      typeDescriptionLink: '#GraphQLOperationType',
      required: true
    },
    identifier: {
      description: 'Operation name, event name, query string, or RegExp used to match requests',
      type: 'GraphQLIdentifier',
      typeDescriptionLink: '#GraphQLIdentifier',
      required: true
    },
    routes: {
      description: 'One or more normalized route configs for this operation',
      type: 'GraphQLRouteConfig[]',
      typeDescriptionLink: '#GraphQLRouteConfig',
      required: true
    },
    interceptors: {
      description: 'Optional interceptors for this specific GraphQL config',
      type: "Interceptors<'graphql'>",
      typeDescriptionLink: './Interceptors'
    }
  }}
/>

<a id='GraphQLRouteConfig'></a>
## GraphQLRouteConfig

<TypeTable
  type={{
    data: {
      description:
        'Static GraphQL result, resolver function, generator resolver, or polling handler',
      type: 'GraphQLDataResponse',
      typeDescriptionLink: '#GraphQLDataResponse',
      required: true
    },
    entities: {
      description:
        'Normalized matcher entities. In operation helpers, pass these as `settings.match`.',
      type: 'GraphQLEntitiesByEntityName',
      typeDescriptionLink: '#GraphQLEntitiesByEntityName'
    },
    settings: {
      description: 'Optional response settings such as delay and status',
      type: 'GraphQLSettings',
      typeDescriptionLink: '#GraphQLSettings'
    },
    interceptors: {
      description: 'Optional interceptors applied to GraphQL routes',
      type: "Interceptors<'graphql'>",
      typeDescriptionLink: './Interceptors'
    }
  }}
/>

Operation helpers accept several config forms and normalize them into `GraphQLRouteConfig.data`.

<TypeTable
  type={{
    'graphql.polling(items)': {
      description: 'Creates a polling response config from ordered response or handler items',
      type: 'Array<{ response: GraphQLExecutionResult; time?: number } | { handler: Function; time?: number }>'
    }
  }}
/>

<a id='GraphQLDataResponse'></a>
## GraphQLDataResponse

<TypeTable
  type={{
    GraphQLDataResponse: {
      description: 'Static GraphQL result, resolver function, or generator resolver',
      type: 'GraphQLExecutionResult | (params: GraphQLParams) => GraphQLExecutionResult | Promise<GraphQLExecutionResult> | Generator'
    }
  }}
/>

<a id='GraphQLSettings'></a>
## GraphQLSettings

<TypeTable
  type={{
    delay: {
      description: 'Optional artificial delay (ms) applied to this response',
      type: 'number'
    },
    status: {
      description: 'Optional HTTP status code for the response',
      type: 'number'
    },
    match: {
      description:
        'Public operation-helper matcher from the optional third argument. It is normalized to `GraphQLRouteConfig.entities`.',
      type: 'GraphQLEntitiesByEntityName',
      typeDescriptionLink: '#GraphQLEntitiesByEntityName'
    }
  }}
/>

<a id='GraphQLOperationType'></a>
## GraphQLOperationType

Operation types supported by GraphQL configs

```typescript
type GraphQLOperationType = 'mutation' | 'query';
```

<a id='GraphQLIdentifier'></a>
## GraphQLIdentifier

Operation identifier

```typescript
type GraphQLIdentifier = string | RegExp;
```

<a id='GraphQLEntitiesByEntityName'></a>
## GraphQLEntitiesByEntityName

```typescript
{
  cookies?: GraphQLEntity<'cookies'>;
  headers?: GraphQLEntity<'headers'>;
  queries?: GraphQLEntity<'queries'>;
  variables?: GraphQLEntity<'variables'>;
}
```
